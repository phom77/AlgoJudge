# Development problem fixtures

These packages exist only to bootstrap a local AlgoJudge database. Their judge
cases are intentionally visible in this repository and must not be reused as
production hidden tests.

Run `./scripts/seed-dev-content.ps1` from the repository root to package,
validate, import, and publish the fixtures.
