Given an array of integers and a target integer, find the two distinct array
elements whose sum equals the target.

Print their zero-based indices in ascending order. Exactly one valid pair exists.

## Input

The first line contains an integer `n`, the number of elements.

The second line contains `n` space-separated integers.

The third line contains the target integer.

## Output

Print two zero-based indices separated by one space.
