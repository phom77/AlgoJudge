# PostgreSQL Operations

EF Core migrations remain in `src/AlgoJudge.Infrastructure/Migrations`.

This directory is reserved for backup/restore runbooks, local initialization
that cannot be represented by EF migrations, and database monitoring notes.
