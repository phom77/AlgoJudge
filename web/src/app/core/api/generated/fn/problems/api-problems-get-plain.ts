/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DifficultyLevel } from '../../models/difficulty-level';
import { PagedResponseOfProblemListItemResponse } from '../../models/paged-response-of-problem-list-item-response';

export interface ApiProblemsGet$Plain$Params {
  Difficulty?: DifficultyLevel;
  PageNumber?: (number | string);
  PageSize?: (number | string);
  Search?: string;
  Solved?: boolean;
  Tags?: Array<string>;
}

export function apiProblemsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiProblemsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfProblemListItemResponse>> {
  const rb = new RequestBuilder(rootUrl, apiProblemsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('Difficulty', params.Difficulty, {});
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('Search', params.Search, {});
    rb.query('Solved', params.Solved, {});
    rb.query('Tags', params.Tags, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfProblemListItemResponse>;
    })
  );
}

apiProblemsGet$Plain.PATH = '/api/problems';
