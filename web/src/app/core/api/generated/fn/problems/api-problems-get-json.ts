/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DifficultyLevel } from '../../models/difficulty-level';
import { PagedResponseOfProblemListItemResponse } from '../../models/paged-response-of-problem-list-item-response';

export interface ApiProblemsGet$Json$Params {
  Difficulty?: DifficultyLevel;
  PageNumber?: (number | string);
  PageSize?: (number | string);
  Search?: string;
  Solved?: boolean;
  Tags?: Array<string>;
}

export function apiProblemsGet$Json(http: HttpClient, rootUrl: string, params?: ApiProblemsGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfProblemListItemResponse>> {
  const rb = new RequestBuilder(rootUrl, apiProblemsGet$Json.PATH, 'get');
  if (params) {
    rb.query('Difficulty', params.Difficulty, {});
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('Search', params.Search, {});
    rb.query('Solved', params.Solved, {});
    rb.query('Tags', params.Tags, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfProblemListItemResponse>;
    })
  );
}

apiProblemsGet$Json.PATH = '/api/problems';
