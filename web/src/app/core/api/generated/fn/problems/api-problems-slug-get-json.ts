/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProblemDetailResponse } from '../../models/problem-detail-response';

export interface ApiProblemsSlugGet$Json$Params {
  slug: string;
}

export function apiProblemsSlugGet$Json(http: HttpClient, rootUrl: string, params: ApiProblemsSlugGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ProblemDetailResponse>> {
  const rb = new RequestBuilder(rootUrl, apiProblemsSlugGet$Json.PATH, 'get');
  if (params) {
    rb.path('slug', params.slug, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProblemDetailResponse>;
    })
  );
}

apiProblemsSlugGet$Json.PATH = '/api/problems/{slug}';
