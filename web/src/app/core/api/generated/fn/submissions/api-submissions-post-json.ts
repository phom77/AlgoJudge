/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateSubmissionRequest } from '../../models/create-submission-request';
import { SubmissionResponse } from '../../models/submission-response';

export interface ApiSubmissionsPost$Json$Params {
      body: CreateSubmissionRequest
}

export function apiSubmissionsPost$Json(http: HttpClient, rootUrl: string, params: ApiSubmissionsPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<SubmissionResponse>> {
  const rb = new RequestBuilder(rootUrl, apiSubmissionsPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'text/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SubmissionResponse>;
    })
  );
}

apiSubmissionsPost$Json.PATH = '/api/submissions';
