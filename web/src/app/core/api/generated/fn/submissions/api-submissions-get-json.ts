/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PagedResponseOfSubmissionResponse } from '../../models/paged-response-of-submission-response';
import { SubmissionStatus } from '../../models/submission-status';

export interface ApiSubmissionsGet$Json$Params {
  PageNumber?: (number | string);
  PageSize?: (number | string);
  ProblemId?: (number | string);
  Status?: SubmissionStatus;
}

export function apiSubmissionsGet$Json(http: HttpClient, rootUrl: string, params?: ApiSubmissionsGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<PagedResponseOfSubmissionResponse>> {
  const rb = new RequestBuilder(rootUrl, apiSubmissionsGet$Json.PATH, 'get');
  if (params) {
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('ProblemId', params.ProblemId, {});
    rb.query('Status', params.Status, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PagedResponseOfSubmissionResponse>;
    })
  );
}

apiSubmissionsGet$Json.PATH = '/api/submissions';
